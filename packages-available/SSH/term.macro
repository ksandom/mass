# Open a terminal to every host in the results with inline syntax. NOTE This will replace --term when it's finished ~ terminal,user
#onLoaded setFeatureType term,actOnHosts

toString ~!Terminal,GUICMD!~
cleanUnresolvedResultVars
exec
getLastList
