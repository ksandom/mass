# The help to display if a "" ~ default

help ~!Help,default!~
outNow

getTags
templateOut tags

# When this script gets called with --helpDefault out() gets called automatically. When it gets called because no parameters have been called out() doesn't get triggered. The following two lines give the macro consistent behavior in both cases.
outNow
clear
nested
