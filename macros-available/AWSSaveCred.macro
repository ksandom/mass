# Not ready. Use --AWSSetCred instead. Save your amazon credentials --AWSSaveCred=accountName,AWSKey,AWSSecret ~ AWS,NOTREADY

# Note that dev is the only account name that can be used at the moment.

set AWSCreds,~!Global,AWSSaveCred-0!~,~!Global,AWSSaveCred1!~,~!Global,AWSSaveCred2!~
saveStoreToData AWSCreds
